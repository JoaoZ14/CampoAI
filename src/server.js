/**
 * Entrada Vercel quando Root Directory = `src` (path0 = pasta src).
 * @see https://vercel.com/docs/frameworks/backend/express
 */
import { createApp } from './expressApp.js';

export default createApp();
